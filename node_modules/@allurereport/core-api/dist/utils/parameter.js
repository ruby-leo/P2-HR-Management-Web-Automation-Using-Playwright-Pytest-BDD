const maskedParameterPlaceholder = "<masked>";
export const redactParameters = (parameters) => {
    return (parameters ?? [])
        .filter((parameter) => !parameter.hidden)
        .map((parameter) => ({
        ...parameter,
        value: parameter.masked ? maskedParameterPlaceholder : parameter.value,
    }));
};
